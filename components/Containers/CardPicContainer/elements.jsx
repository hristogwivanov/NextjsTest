import styled from "styled-components";

export const StyledCardPicContainer = styled(({ ...props }) => (
    <div {...props} />
))`
    margin: auto;
    margin-right: 3.2rem;
    margin-left: 3.2rem;
`;
